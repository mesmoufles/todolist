{% extends 'base.html.twig' %}

{% block body %}
    <header>
        <h1>New user</h1>
        <nav>
            <ul>
                <li>
                    <a href="{{ path('user_index') }}">Back to list</a>
                </li>
            </ul>
        </nav>
    </header>

    <div class="general-wrapper">
        <div class="form-container">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="submit-container">
                <input type="submit" value="Create" id="new-user-submit"/>
                {{ form_end(form) }}
            </div>
        </div>
    </div>


{% endblock %}
